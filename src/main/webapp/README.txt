In order to integrate nicely into Eclipse 3.3 as a Dynamic Web Project, Maven 2's 
default webapp directory (PROJECT_HOME/src/main/webapp) cannot be used. Instead
the directory PROJECT_HOME/webapp must be used.